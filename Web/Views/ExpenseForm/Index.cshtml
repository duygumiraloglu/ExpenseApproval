@using Web.Models
@model List<ExpenseForm>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/ExpenseForm.cshtml";
}

<br />

<div class="form-group">
    <a href="/ExpenseForm/AddExpenseForm/" class="btn btn-primary">Ekle</a>
</div>
<!-- Masraf kayıtlarını listeleyin -->
<table class="table table-bordered">
    <thead>
        <tr>
            <th>Tarih</th>
            <th>Durum</th>
            <th>Toplam Masraf</th>
            <th>Detay</th>
            <th>Sil</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var expenseForm in Model)
        {
            <tr>
                <td>@expenseForm.CreatedDate</td>
                <td>@expenseForm.Status</td>
                <td>@expenseForm.TotalAmount</td>
                <td><input type="submit" value="Düzenle" class="btn btn-success" /></td>
                <td><input type="submit" value="Sil" class="btn btn-dan" /></td>
            </tr>
        }
    </tbody>
</table>
